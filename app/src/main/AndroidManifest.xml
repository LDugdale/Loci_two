<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lauriedugdale.loci">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:theme="@style/FullScreenTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <service
            android:name=".services.LociLocationService"
            android:stopWithTask="true" />

        <activity android:name=".ui.activity.auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.auth.SignupActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".ui.activity.auth.ResetPasswordActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".ui.activity.UploadActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".ui.activity.entry.ImageEntryActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".ui.activity.FullScreenActivity"
            android:theme="@style/FullScreenTheme" />

        <service
            android:name=".services.AudioService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".ui.activity.entry.AudioEntryActivity" />
        <!-- <activity -->
        <!-- android:name=".NavDraw" -->
        <!-- android:label="@string/title_activity_nav_draw" -->
        <!-- android:theme="@style/AppTheme" /> -->
        <activity android:name=".ui.activity.SelectFriend" />
        <activity android:name=".ui.activity.NotificationActivity" />
        <activity android:name=".ui.activity.AugmentedActivity" />
        <activity
            android:name=".ui.CreateGroup"
            android:label="@string/title_activity_create_group"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.activity.creategroup.CreateGroup" />
        <activity android:name=".ui.activity.entry.NoMediaActivity" />
        <activity android:name=".ui.activity.social.UserProfileActivity" />

        <service
            android:name=".services.GeoFencingService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".services.GeofenceIntentService"
            android:exported="false"></service>
    </application>

</manifest>